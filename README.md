Boilerplace for running fastapi with postgres.

Includes:
 - Postgres
 - FastAPI with some example models/crud
 - Docker
 - Test DB
 - Basic tests

Run app:
```shell
docker-compose up --build backend
```

Run tests:
```shell
pytest
```